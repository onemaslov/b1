# Локальная разработка с SQLite

## 📋 Текущая конфигурация

✅ **Локально:** SQLite (файл `dev.db`)
✅ **На Vercel:** PostgreSQL (Supabase) - позже настроим

## 🚀 Запуск приложения локально

### Шаг 1: Настроить переменные окружения

Создайте файл `.env.local` в корне проекта:

```
DATABASE_URL="file:./dev.db"
```

**Важно:** Используйте `file:./dev.db` (так Prisma знает что это SQLite)

### Шаг 2: Установить зависимости

```bash
npm install
```

### Шаг 3: Сгенерировать Prisma Client

```bash
npx prisma generate
```

### Шаг 4: Применить миграции (создать таблицы)

```bash
npx prisma migrate dev
```

### Шаг 5: Запустить сервер разработки

```bash
npm run dev
```

Откройте http://localhost:3000

## 📝 Тестирование

### 1. Авторизация

1. Откройте http://localhost:3000/login
2. Введите:
   - Логин: `admin`
   - Пароль: `qwerty`
3. Нажмите "Войти"

### 2. Добавить метку

1. Кликните на карту
2. Введите название метки
3. Нажмите "Сохранить"
4. **Метка должна появиться на карте**

### 3. Проверить что метка сохранилась

**Вариант А (через БД):**
```bash
sqlite3 dev.db "SELECT * FROM markers WHERE userId='admin123' ORDER BY createdAt DESC LIMIT 1;"
```

**Вариант Б (через API):**
```bash
# Откройте DevTools (F12) → Network
# Кликните на GET /api/markers
# Вы должны увидеть ваши метки в ответе
```

### 4. Перезагрузить страницу (F5)

- Метка должна остаться на карте!
- Если остается - ✅ всё работает!
- Если исчезает - ❌ есть проблема

## 🔍 Отладка проблем

### Проблема: Метки не сохраняются

**Шаг 1: Проверьте логи сервера**
- В терминале где запущен `npm run dev` должны быть логи
- Ищите ошибки (красный текст)

**Шаг 2: Проверьте DevTools браузера**
- F12 → Console
- Есть ли красные ошибки?
- F12 → Network → POST /api/markers
  - Какой статус кода? (должен быть 201)
  - Что в Response?

**Шаг 3: Проверьте БД напрямую**
```bash
sqlite3 dev.db "SELECT COUNT(*) FROM markers;"
```
Должно показать количество всех меток.

**Шаг 4: Проверьте что userId правильный**
```bash
sqlite3 dev.db "SELECT * FROM markers LIMIT 1;"
```
Посмотрите последнее поле - это userId, должно быть `admin123`

### Проблема: "Cannot find module '@prisma/client'"

```bash
npx prisma generate
npm install
```

### Проблема: "SQLITE_CANTOPEN"

Проверьте `.env.local`:
```bash
cat .env.local
```

Должно быть:
```
DATABASE_URL="file:./dev.db"
```

Не `file:dev.db` и не просто `dev.db`

### Проблема: Таблицы не созданы

```bash
npx prisma migrate dev --name init
```

Это создаст таблицы.

## ✅ Финальная проверка

```bash
# 1. Установка
npm install

# 2. Генерация Prisma Client
npx prisma generate

# 3. Миграция БД
npx prisma migrate dev

# 4. Запуск
npm run dev

# 5. Откройте http://localhost:3000
# 6. Логин: admin, Пароль: qwerty
# 7. Добавьте метку
# 8. F5 (перезагрузка)
# 9. Метка должна остаться!
```

## 📊 Как БД работает локально

```
SQLite File (dev.db)
    ↓
Prisma ORM (преобразует в запросы)
    ↓
Next.js API Routes
    ↓
React Component (отображает на экране)
```

## 🔄 Когда переходить на PostgreSQL

После того как локально всё работает:
1. Создать Supabase БД
2. Получить Connection String
3. Добавить в Vercel Environment Variables
4. Изменить provider на "postgresql" в schema.prisma
5. Деплоить на Vercel

**Но пока - только SQLite локально!**

## 📞 Полезные команды

```bash
# Запустить сервер разработки
npm run dev

# Открыть Prisma Studio (UI для БД)
npx prisma studio

# Проверить статус миграций
npx prisma migrate status

# Создать новую миграцию
npx prisma migrate dev --name "название"

# Сбросить БД (удалит все данные!)
npx prisma migrate reset

# Просмотреть логи
sqlite3 dev.db ".log on"
```

## 🎯 Должно быть работает

Если вы всё сделали как описано выше - всё должно работать:
- ✅ Добавить метку
- ✅ Вернуться на сайт
- ✅ Метка есть
- ✅ Перезагрузить (F5)
- ✅ Метка остаётся

Если что-то не работает - напишите логи из терминала и DevTools.

