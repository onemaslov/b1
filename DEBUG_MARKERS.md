# 🔍 Отладка проблемы с сохранением меток

Я добавил детальные логи в код. Вот как их использовать:

## 🚀 Шаг 1: Перезапустить сервер

```bash
# Ctrl+C чтобы остановить
# Потом:
npm run dev
```

## 🔍 Шаг 2: Смотреть логи

В двух местах:

### 1️⃣ Логи Сервера (терминал)
Когда вы добавляете метку, смотрите в терминале:

```
📍 [POST /api/markers] Запрос получен
🔐 userId из cookie: admin123
📦 Получены данные: { title: 'тест', latitude: 55.75, longitude: 37.62 }
💾 Сохраняю метку в БД с userId: admin123
✅ Метка успешно сохранена: abc123def
```

Или если ошибка:
```
❌ Ошибка при создании метки: ...
❌ Ошибка детали: Foreign key constraint violated
```

### 2️⃣ Логи Браузера (DevTools)

`F12 → Console`

Когда вы добавляете метку, смотрите:

```
📍 [Frontend] Отправляю метку: {title: 'тест', latitude: 55.75, longitude: 37.62}
✨ Создание новой метки
📨 Ответ от сервера: 201
✅ Метка создана успешно
📥 Загружаю метки...
📨 Статус ответа: 200
✅ Метки загружены: 1 шт
```

## 🎯 Полный сценарий отладки

```
1. Откройте DevTools браузера (F12)
2. Перейдите в Console (F12 → Console)
3. Откройте другой терминал чтобы видеть сервер
4. Авторизуйтесь (admin/qwerty)
5. Кликните на карту
6. Заполните форму метки
7. Нажмите "Сохранить"
8. Смотрите логи в DevTools и терминале
9. Скопируйте ВСЕ логи и скажите мне
```

## 📊 Логи которые должны быть

### ✅ Успешное добавление:

**Браузер (DevTools Console):**
```
📍 [Frontend] Отправляю метку: {...}
✨ Создание новой метки
📨 Ответ от сервера: 201
✅ Метка создана успешно
📥 Загружаю метки...
✅ Метки загружены: 1 шт
```

**Сервер (Terminal):**
```
📍 [POST /api/markers] Запрос получен
🔐 userId из cookie: admin123
📦 Получены данные: {...}
💾 Сохраняю метку в БД с userId: admin123
✅ Метка успешно сохранена: xyz789
```

### ❌ Ошибка авторизации (401):

**Браузер:**
```
📨 Ответ от сервера: 401
```

**Сервер:**
```
🔐 userId из cookie: undefined
❌ userId отсутствует в cookie
```

Решение: вы не авторизованы, логин: admin, пароль: qwerty

### ❌ Ошибка БД (500):

**Браузер:**
```
📨 Ответ от сервера: 500
❌ Ошибка создания: {error: "...", details: "Foreign key constraint violated"}
```

**Сервер:**
```
❌ Ошибка при создании метки: PrismaClientKnownRequestError
❌ Ошибка детали: Foreign key constraint violated
```

Это означает что userId не найден в таблице users.

## 📞 Важно!

Когда попытаетесь добавить метку:
1. Смотрите консоль браузера (F12 → Console)
2. Смотрите терминал где запущен `npm run dev`
3. Скопируйте ВСЕ логи (с 📍, ✅, ❌ и т.д.)
4. Отправьте мне вместе с ответом

Это поможет сразу понять в чем проблема!

