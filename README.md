# Карта с метками

Интерактивное веб-приложение для работы с картой и метками. Позволяет добавлять, редактировать и удалять метки на карте с удобным интерфейсом.

## 🚀 Технологии

- **Frontend**: Next.js 14, React 18, TypeScript
- **Стилизация**: Tailwind CSS
- **Карты**: Leaflet + OpenStreetMap
- **База данных**: PostgreSQL + Prisma ORM
- **Иконки**: Lucide React
- **Хостинг**: Vercel

## ✨ Функциональность

- ✅ Интерактивная карта с поддержкой OpenStreetMap
- ✅ **Автоматическое определение местоположения пользователя**
- ✅ **Кнопка для быстрого перехода к текущему местоположению**
- ✅ Добавление меток на карту кликом
- ✅ Редактирование существующих меток
- ✅ Удаление меток с подтверждением
- ✅ Просмотр списка всех меток в сайдбаре
- ✅ Центрирование карты на выбранной метке
- ✅ Уведомления об ошибках (Toast)
- ✅ Полностью русскоязычный интерфейс
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ REST API для работы с метками

## 📋 Требования

- **Node.js 18+** (⚠️ ВАЖНО: Версия 14 не поддерживается!)
- npm или yarn
- PostgreSQL (или SQLite для локальной разработки)

> **🚨 Если у вас Node.js 14 или ниже**, см. файл [SETUP_INSTRUCTIONS.md](./SETUP_INSTRUCTIONS.md) для инструкций по обновлению

## 🛠️ Установка и запуск локально

### 1. Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd b1
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка базы данных

Создайте файл `.env` в корне проекта:

```env
# Для PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/map_markers_db?schema=public"

# Или для SQLite (проще для локальной разработки)
DATABASE_URL="file:./dev.db"
```

**Для SQLite** измените также файл `prisma/schema.prisma`:
```prisma
datasource db {
  provider = "sqlite"  // вместо "postgresql"
  url      = env("DATABASE_URL")
}
```

### 4. Инициализация базы данных

```bash
# Создание миграций
npx prisma migrate dev --name init

# Генерация Prisma Client
npx prisma generate
```

### 5. Запуск сервера разработки

```bash
npm run dev
```

Откройте браузер и перейдите на [http://localhost:3000](http://localhost:3000)

## 📦 Сборка для production

```bash
npm run build
npm start
```

## 🌐 Деплой на Vercel

### Автоматический деплой

1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Подключите свой GitHub репозиторий
3. Настройте переменные окружения в настройках проекта Vercel:
   - `DATABASE_URL` - URL вашей базы данных

### База данных для production

Рекомендуется использовать один из следующих сервисов:

**Supabase** (рекомендуется):
1. Создайте проект на [Supabase](https://supabase.com)
2. Скопируйте Connection String из Settings → Database
3. Добавьте в переменные окружения Vercel

**PlanetScale**:
1. Создайте базу на [PlanetScale](https://planetscale.com)
2. Получите URL подключения
3. Добавьте в переменные окружения Vercel

**Railway**:
1. Создайте PostgreSQL базу на [Railway](https://railway.app)
2. Скопируйте DATABASE_URL
3. Добавьте в Vercel

### CI/CD

Vercel автоматически деплоит проект при каждом push в ветку `main`:
- Pull Request создаёт preview deployment
- Merge в main деплоит в production

## 🗂️ Структура проекта

```
b1/
├── prisma/
│   └── schema.prisma          # Схема базы данных
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   └── markers/       # API endpoints для меток
│   │   ├── globals.css        # Глобальные стили
│   │   ├── layout.tsx         # Корневой layout
│   │   └── page.tsx           # Главная страница
│   ├── components/
│   │   ├── Map.tsx            # Компонент карты
│   │   ├── Sidebar.tsx        # Сайдбар со списком меток
│   │   ├── MarkerModal.tsx    # Модальное окно метки
│   │   └── DeleteConfirmModal.tsx # Подтверждение удаления
│   ├── lib/
│   │   └── prisma.ts          # Prisma клиент
│   └── types/
│       └── marker.ts          # TypeScript типы
├── .eslintrc.json
├── .gitignore
├── next.config.js
├── package.json
├── postcss.config.js
├── tailwind.config.ts
├── tsconfig.json
└── vercel.json
```

## 📡 API Endpoints

### Получить все метки
```
GET /api/markers
```

### Получить метку по ID
```
GET /api/markers/[id]
```

### Создать метку
```
POST /api/markers
Body: {
  "title": "Название",
  "description": "Описание (опционально)",
  "latitude": 55.7558,
  "longitude": 37.6173
}
```

### Обновить метку
```
PATCH /api/markers/[id]
Body: {
  "title": "Новое название",
  "description": "Новое описание",
  "latitude": 55.7558,
  "longitude": 37.6173
}
```

### Удалить метку
```
DELETE /api/markers/[id]
```

## 🔧 Полезные команды

```bash
# Запуск в режиме разработки
npm run dev

# Сборка для production
npm run build

# Запуск production сборки
npm start

# Линтинг кода
npm run lint

# Просмотр базы данных в Prisma Studio
npx prisma studio

# Создание новой миграции
npx prisma migrate dev --name <название>

# Применение миграций в production
npx prisma migrate deploy
```

## 🐛 Решение проблем

### Ошибка при запуске: "Cannot find module '@prisma/client'"

```bash
npx prisma generate
```

### Карта не отображается

1. Проверьте, что Leaflet CSS подключен
2. Убедитесь, что компонент карты импортирован с `ssr: false`

### Геолокация не работает

1. **Проверьте разрешения браузера**: Убедитесь, что браузер имеет доступ к геолокации
2. **HTTPS требуется**: Геолокация работает только на HTTPS (кроме localhost)
3. **Проверьте настройки**: В некоторых браузерах нужно явно разрешить доступ к местоположению

### Ошибки базы данных

```bash
# Сброс базы данных (ОСТОРОЖНО: удалит все данные!)
npx prisma migrate reset

# Пересоздание Prisma Client
npx prisma generate
```

## 📝 Лицензия

MIT

## 👤 Автор

Разработано с использованием современных веб-технологий.

