# Система авторизации - Реализация

## ✅ Что реализовано

### 1. База данных
- **Модель User** с полями:
  - `id` - уникальный идентификатор
  - `username` - логин (уникальный)
  - `password` - пароль (простое хранение для разработки)
  - `createdAt`, `updatedAt` - временные метки

- **Связь User ↔ Marker**:
  - Один пользователь может иметь много меток
  - Каждая метка принадлежит одному пользователю
  - При удалении пользователя удаляются все его метки (CASCADE)

### 2. API Endpoints

#### Авторизация
- `POST /api/auth/login` - вход в систему
  - Принимает: `{ username, password }`
  - Возвращает: информацию о пользователе
  - Устанавливает cookie с userId

- `POST /api/auth/logout` - выход из системы
  - Удаляет cookie с userId

- `GET /api/auth/session` - проверка текущей сессии
  - Возвращает информацию о пользователе или 401

#### Метки (обновлено)
Все endpoints меток теперь:
- Требуют авторизации (проверяют cookie userId)
- Фильтруют метки по текущему пользователю
- Возвращают 401 если пользователь не авторизован

### 3. Frontend

#### Страница входа (`/login`)
- Красивая форма входа
- Поля: логин и пароль
- Отображение ошибок
- Индикатор загрузки
- Подсказка с тестовыми данными
- Автоматическое перенаправление на главную после входа

#### Главная страница (обновлено)
- Кнопка "Выход" в хедере
- Обработчик выхода с перенаправлением на `/login`

#### Middleware
- Защита главной страницы `/`
- Перенаправление неавторизованных на `/login`
- Перенаправление авторизованных с `/login` на `/`

### 4. Seed данные
- Скрипт `prisma/seed.ts` для создания тестового пользователя
- Логин: `admin`
- Пароль: `qwerty`

## 📋 Команды для применения изменений

### Шаг 1: Установить зависимости
```bash
npm install
```

### Шаг 2: Применить миграцию
```bash
npx prisma migrate dev --name add_auth
```

Эта команда создаст:
- Таблицу `users`
- Добавит поле `userId` в таблицу `markers`
- Применит изменения к базе данных

### Шаг 3: Создать тестового пользователя
```bash
npm run db:seed
```

Или:
```bash
npx tsx prisma/seed.ts
```

### Шаг 4: Перезапустить сервер
```bash
# Остановить текущий сервер (Ctrl+C)
npm run dev
```

## 🎯 Как использовать

1. Откройте [http://localhost:3000](http://localhost:3000)
2. Вы будете перенаправлены на `/login`
3. Введите:
   - Логин: `admin`
   - Пароль: `qwerty`
4. Нажмите "Войти"
5. Вы попадёте на главную страницу с картой
6. Все созданные метки будут привязаны к вашему аккаунту
7. Для выхода нажмите кнопку "Выход" справа вверху

## 🔒 Безопасность

### Текущая реализация (для разработки):
- ✅ Простое сравнение паролей
- ✅ Cookie-based сессии
- ✅ HttpOnly cookies
- ✅ Защита роутов через middleware

### Для production рекомендуется:
- 🔐 Хеширование паролей (bcrypt, argon2)
- 🔑 JWT токены вместо простых cookies
- 🛡️ CSRF защита
- 🔒 HTTPS обязательно
- 🔄 Refresh tokens
- ⏱️ Ограничение попыток входа (rate limiting)
- 📧 Email верификация
- 🔔 2FA (двухфакторная аутентификация)

## 📝 Структура файлов

### Новые файлы:
```
src/
├── app/
│   ├── api/
│   │   └── auth/
│   │       ├── login/route.ts      # Вход
│   │       ├── logout/route.ts     # Выход
│   │       └── session/route.ts    # Проверка сессии
│   └── login/
│       └── page.tsx                # Страница входа
├── middleware.ts                   # Защита роутов
prisma/
└── seed.ts                        # Seed данные

```

### Изменённые файлы:
```
src/app/page.tsx                   # Добавлена кнопка выхода
src/app/api/markers/route.ts       # Добавлена проверка авторизации
src/app/api/markers/[id]/route.ts  # Добавлена проверка авторизации
prisma/schema.prisma               # Добавлены User и связь с Marker
package.json                       # Добавлена команда db:seed
```

## 🧪 Тестирование

### Вход
1. Перейти на `/login`
2. Ввести admin / qwerty
3. ✅ Должен произойти вход и перенаправление на `/`

### Защита главной страницы
1. Выйти из системы
2. Попробовать открыть `/`
3. ✅ Должно перенаправить на `/login`

### Метки
1. Войти как admin
2. Создать несколько меток
3. Выйти и войти снова
4. ✅ Метки должны остаться

### Изоляция данных
На данный момент только один пользователь (admin), но если создать второго:
- Каждый пользователь видит только свои метки
- Нельзя редактировать/удалить чужие метки

## 🚀 Готово к использованию

Система авторизации полностью функциональна и готова к использованию в режиме разработки!

