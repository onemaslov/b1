# 🚨 Важная информация по установке

## Проблема с версией Node.js

Ваша текущая версия Node.js: **v14.21.3**

Для работы проекта требуется **Node.js 18 или выше**.

## 🔧 Решение: Обновите Node.js

### Вариант 1: Использование nvm (Рекомендуется)

```bash
# Установите nvm (если ещё не установлен)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезапустите терминал или выполните:
source ~/.zshrc

# Установите Node.js 18
nvm install 18

# Используйте Node.js 18
nvm use 18

# Сделайте версию 18 по умолчанию
nvm alias default 18

# Проверьте версию
node --version  # Должно показать v18.x.x
```

### Вариант 2: Прямая установка

Скачайте и установите Node.js 18+ с официального сайта:
[https://nodejs.org/](https://nodejs.org/)

Выберите **LTS версию** (Long Term Support).

---

## ⚡ После обновления Node.js

### 1. Установите зависимости

```bash
cd /Users/cooper16/Senor/b1
npm install
```

### 2. Создайте файл .env

```bash
echo 'DATABASE_URL="file:./dev.db"' > .env
```

### 3. Инициализируйте базу данных

```bash
npx prisma generate
npx prisma migrate dev --name init
```

### 4. Запустите проект

```bash
npm run dev
```

Откройте браузер и перейдите на [http://localhost:3000](http://localhost:3000)

---

## 🎯 Быстрый старт (одной командой)

После обновления Node.js выполните:

```bash
cd /Users/cooper16/Senor/b1 && \
npm install && \
echo 'DATABASE_URL="file:./dev.db"' > .env && \
npx prisma generate && \
npx prisma migrate dev --name init && \
npm run dev
```

---

## 🐛 Возможные проблемы и решения

### npm install зависает или выдаёт ошибки

```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Установите заново
npm install
```

### Ошибка "Cannot find module @prisma/client"

```bash
npx prisma generate
```

### Ошибка подключения к базе данных

Убедитесь, что файл `.env` создан с правильным содержимым:

```bash
cat .env
# Должно показать: DATABASE_URL="file:./dev.db"
```

Если файла нет, создайте:

```bash
echo 'DATABASE_URL="file:./dev.db"' > .env
```

---

## 📚 Дополнительная информация

- Полная документация в файле [README.md](./README.md)
- Инструкции по деплою в файле [DEPLOYMENT.md](./DEPLOYMENT.md)
- Описание структуры проекта в файле [instruction.md](./instruction.md)

---

## ✅ Проверка корректной установки

После установки проверьте:

1. **Node.js версия 18+**
   ```bash
   node --version
   ```

2. **Зависимости установлены**
   ```bash
   ls node_modules | wc -l
   # Должно быть больше 500
   ```

3. **Prisma клиент сгенерирован**
   ```bash
   ls node_modules/.prisma/client
   # Должна быть директория с файлами
   ```

4. **База данных создана**
   ```bash
   ls *.db
   # Должен быть файл dev.db
   ```

5. **Сервер запускается**
   ```bash
   npm run dev
   # Должно показать: ready - started server on 0.0.0.0:3000
   ```

---

## 💡 Альтернатива: Docker

Если не хотите обновлять Node.js на вашей системе, можете использовать Docker:

```bash
# Создайте Dockerfile (уже есть в документации DEPLOYMENT.md)
# Затем запустите:

docker-compose up
```

Это создаст изолированное окружение с правильной версией Node.js.

