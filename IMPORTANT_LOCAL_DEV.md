# 🔧 Переключение между SQLite (локально) и PostgreSQL (прод)

## Вариант 1: Два файла schema (рекомендуется для прода)

Для деплоя используйте `schema.prisma` с PostgreSQL.
Для локальной разработки держите `schema.local.prisma` с SQLite.

## Вариант 2: Использовать только PostgreSQL везде

Установите PostgreSQL локально или используйте Supabase для разработки.

## ⚠️ Текущая ситуация:

- `schema.prisma` настроен на **PostgreSQL** (для прода)
- Локально пока работает SQLite благодаря `PRAGMA foreign_keys OFF`
- При следующем `npx prisma generate` может сломаться локально!

## ✅ Что нужно сделать ПЕРЕД деплоем:

1. **НЕ запускайте `npx prisma generate` локально** пока не задеплоите
2. Просто закоммитьте изменения и задеплойте.
3. На Vercel всё соберётся правильно с PostgreSQL

## 🚀 Деплой прямо сейчас:

```bash
git add .
git commit -m "Готов к деплою: PostgreSQL + миграции"
git push
```

После деплоя не забудьте добавить admin пользователя в Supabase (см. DEPLOY_GUIDE.md)!

